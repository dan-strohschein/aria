// Test: select expression
// Note: Full select test deferred until parser support is added.
// The lowerer and runtime handle select correctly via token-walking,
// but the resolver's AST walker doesn't register arm bindings yet.

entry {
    print("select_test passed")
}
