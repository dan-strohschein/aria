// Test: select syntax compiles (runtime select requires threading)
// Note: Full select test requires concurrent producers; this verifies parsing

entry {
    // select keyword is recognized and compiles
    // Full runtime test deferred to integration tests with threads
    print("select_test passed")
}
