// [generated] Layer 1 mechanical extraction — not yet reviewed

@module property
@lang aria
@version 0.1.0
@aid_version 0.2

---

@fn forAll_bool
@purpose Tests a property over generated bools.
@sig (prop: fn(bool) -> bool, seed: i64, count: i64) -> i64
@params
  prop: fn(bool) -> bool
  seed: i64
  count: i64
@calls [assert, gen_bool, lcg_next, prop]
@source_file property.aria
@source_line 71

---

@fn forAll_i64
@purpose Tests a property over `count` randomly generated i64 values.
@sig (prop: fn(i64) -> bool, seed: i64, count: i64) -> i64
@params
  prop: fn(i64) -> bool
  seed: i64
  count: i64
@calls [assert, gen_i64_small, lcg_next, prop]
@source_file property.aria
@source_line 58

---

@fn forAll_pair
@purpose Tests a 2-argument property over pairs of i64 values.
@sig (prop: fn(i64, i64) -> bool, seed: i64, count: i64) -> i64
@params
  prop: fn(i64, i64) -> bool
  seed: i64
  count: i64
@calls [assert, gen_i64_small, lcg_next, prop]
@source_file property.aria
@source_line 84

---

@fn gen_bool
@purpose Generates a bool
@sig (seed: i64) -> bool
@params
  seed: i64
@calls [lcg_next]
@source_file property.aria
@source_line 48

---

@fn gen_i64_small
@purpose Generates an i64 in [-1000, 1000)
@sig (seed: i64) -> i64
@params
  seed: i64
@calls [lcg_next]
@source_file property.aria
@source_line 40

---

@fn gen_range
@purpose Generates an i64 in the range [0, max).
@sig (seed: i64, max: i64) -> i64
@params
  seed: i64
  max: i64
@calls [lcg_next]
@source_file property.aria
@source_line 31

---

@fn lcg_next
@purpose Linear congruential generator: seed → next pseudo-random i64.
@sig (seed: i64) -> i64
@params
  seed: i64
@source_file property.aria
@source_line 20
